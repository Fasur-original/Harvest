{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the operator console and display windows",
  "windows": ["main", "display"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
